/* ACFAN 0.1.0-test.1 terminal playback */
var ACFANPlayback=(function(){
  var C=ACFANCore;
  function bridgeUrl(kind,id,title,chapter){return C.page('acfanBridge',{acf_kind:kind||'video',acf_id:id||'',acf_title:title||'',acf_chapter:chapter||''});}
  function bridgeScript(kind,id,title,chapter){var data={kind:String(kind||''),id:String(id||''),title:String(title||''),chapter:String(chapter||'')};return '('+function(cfg){try{document.documentElement.style.background='#fff';if(document.body)document.body.style.background='#fff';}catch(e){}var tries=0,done=false;function text(el){return String((el&&el.innerText)||'').replace(/\s+/g,' ').trim();}function visible(el){if(!el)return false;var r=el.getBoundingClientRect();return r.width>0&&r.height>0;}function click(el){try{if(el){el.click();return true;}}catch(e){}return false;}function fire(el,type){try{el.dispatchEvent(new Event(type,{bubbles:true}));}catch(e){}}function findText(t){if(!t)return null;var s=t.slice(0,Math.min(14,t.length)),all=document.querySelectorAll('a,button,div,span,p,h1,h2,h3');for(var i=0;i<all.length;i++){var z=text(all[i]);if(z&&z.indexOf(s)>=0&&visible(all[i]))return all[i];}return null;}function tick(){if(done)return;tries++;if(cfg.kind==='video'){var v=document.querySelector('video');if(v&&visible(v)){done=true;try{v.scrollIntoView({block:'start'});v.play();}catch(e){}return;}}if(cfg.kind==='comic'){var imgs=document.querySelectorAll('img');if(imgs&&imgs.length>10){done=true;try{imgs[0].scrollIntoView({block:'start'});}catch(e){}return;}}var hit=findText(cfg.title);if(hit){click((hit.closest&&hit.closest('a'))||hit);if(tries>3)return;}var input=document.querySelector('input[type=search],input[placeholder*="搜索"],input[placeholder*="搜"],input[type=text]');if(!input){var btns=document.querySelectorAll('button,a,div,span,i');for(var b=0;b<btns.length;b++){var q=text(btns[b]),al=String((btns[b].getAttribute&&((btns[b].getAttribute('aria-label')||'')+' '+(btns[b].getAttribute('title')||'')))||'');if((q==='搜索'||/搜索|search/i.test(al))&&visible(btns[b])){click(btns[b]);break;}}}else if(cfg.title&&String(input.value||'')!==cfg.title){try{var setter=Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype,'value').set;setter.call(input,cfg.title);}catch(e2){input.value=cfg.title;}fire(input,'input');fire(input,'change');try{input.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',code:'Enter',keyCode:13,which:13,bubbles:true}));input.dispatchEvent(new KeyboardEvent('keyup',{key:'Enter',code:'Enter',keyCode:13,which:13,bubbles:true}));}catch(e3){}}if(tries>22)clearInterval(timer);}var timer=setInterval(tick,900);setTimeout(tick,350);}+')('+JSON.stringify(data)+');';}
  function directAudio(url,title){url=C.s(url).trim();if(!url)return'toast://没有可用音频';return JSON.stringify({urls:[url],names:[title||'播放'],headers:[{'User-Agent':C.ua}]});}
  return{bridgeUrl:bridgeUrl,bridgeScript:bridgeScript,directAudio:directAudio};
})();
