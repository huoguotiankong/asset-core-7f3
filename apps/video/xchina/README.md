# 小黄书 xChina

海阔视界自用远程版。当前仅发布 Test 通道；实机验证通过前不建立 Stable。

当前 Test：`0.1.0-test.4 / Build 10104`。

内容类型：小说、套图/写真、漫画、视频、业余自拍、模特人物。Test4 为独立 Runtime，按已上传阅读源补齐漫画独立域、CSS 背景封面、章节/图片阅读、套图分页与媒体数组解析，同时保留主域/备用域切换、私有文件 HTML 缓存和限次 WebView 验证兜底。

二级页继续使用 `rule=&simple=true` 与 `xc_url / xc_path` 业务参数命名空间。当前仍需海阔实机验证网络、漫画域、正文图片和播放链，实机闭环前不得晋级 Stable。
