# RC4 实机回归门禁

- 多版本详情必须显示非空 Stable/Test 列表。
- Stable/Test 版本卡必须可导入。
- “打开程序”必须通过真实本地规则 descriptor + `fba.open` 正常进入，不得再使用 X5 `hiker://home@规则名`。
- 至少抽测一个其它 channel-group 程序的版本列表与导入。
- Fast Home 与 Verified Install Index 不得退化。
- 上述全部通过前禁止再次晋级 Stable 3.5.6。
