(function(){
var raw=fetch("hiker://home@115.简");
if(!raw||raw==="null") return "toast://未找到原版115.简，请先保留原版";
var rule;
try{rule=JSON.parse(raw);}catch(e){return "toast://读取原版115.简失败："+e.message;}
var pages;
try{pages=JSON.parse(rule.pages||"[]");}catch(e2){return "toast://解析原版页面失败："+e2.message;}
var offlineRule="js:\n// 115Offline Test3：磁链调用专用体验 + 离线任务管理\nlet d = [];\nlet api = $.require(\"115Api\");\nlet client = api.newClient();\n\nfunction q(name, dflt) {\n    let v = \"\";\n    try { v = String(getParam(name, \"\") || \"\"); } catch (e) { v = \"\"; }\n    if (v) {\n        try { v = decodeURIComponent(v); } catch (e2) { }\n        return v;\n    }\n    try {\n        let mv = (typeof MY_PARAMS !== \"undefined\" && MY_PARAMS) ? MY_PARAMS[name] : undefined;\n        if (mv !== undefined && mv !== null && mv !== \"\") return String(mv);\n    } catch (e3) { }\n    return dflt || \"\";\n}\n\nfunction resolvePage() {\n    let n = 1;\n    try {\n        if (typeof MY_PAGE !== \"undefined\" && MY_PAGE !== null && MY_PAGE !== \"\") {\n            let p = parseInt(MY_PAGE, 10);\n            if (!isNaN(p) && p > 0) n = p;\n        }\n    } catch (e) { }\n    if (n === 1) {\n        try {\n            let p2 = String(getParam(\"page\", \"\") || \"\");\n            if (/^\\d+$/.test(p2) && parseInt(p2, 10) > 0) n = parseInt(p2, 10);\n        } catch (e2) { }\n    }\n    return n;\n}\nlet myPage = resolvePage();\nlet autoAdd = myPage === 1 ? q(\"add\", \"\").trim() : \"\";\nlet focusMode = !!autoAdd;\n\nfunction statusLine(t) {\n    if (t.status === 2) return \"✅ 已完成\";\n    if (t.status === -1) return \"❌ 失败\";\n    if (t.status === 1) {\n        let pct = t.percent > 0 ? (Math.round(t.percent * 100) / 100) + \"%\" : \"\";\n        let rate = t.rateDownload > 0 ? \" · \" + api.tool.formatSize(t.rateDownload) + \"/s\" : \"\";\n        let left = t.leftTime > 0 ? \" · 剩余 \" + Math.ceil(t.leftTime / 60) + \" 分钟\" : \"\";\n        return \"⬇ 下载中 \" + pct + rate + left;\n    }\n    return \"⏳ 等待中\";\n}\n\nfunction btihOf(s) {\n    let m = /(?:^|[?&])xt=urn:btih:([A-Za-z0-9]+)/i.exec(String(s || \"\"));\n    if (!m) return \"\";\n    let h = String(m[1] || \"\").toLowerCase();\n    return /^[0-9a-f]{40}$/.test(h) ? h : \"\";\n}\n\nfunction listTasks(maxPages) {\n    let out = [], seen = {};\n    for (let p = 1; p <= maxPages; p++) {\n        let r = client.listOfflineTask(p);\n        let a = (r && r.tasks) || [];\n        if (!a.length) break;\n        let sig = a.length + \"|\" + (a[0] ? a[0].infoHash : \"\");\n        if (seen[sig]) break;\n        seen[sig] = true;\n        a.forEach((x) => out.push(x));\n        if (r.pageCount && p >= r.pageCount) break;\n    }\n    return out;\n}\n\nfunction findSameTask(tasks, link) {\n    let raw = String(link || \"\").trim();\n    let hh = btihOf(raw);\n    for (let i = 0; i < tasks.length; i++) {\n        let t = tasks[i] || {};\n        if (hh && String(t.infoHash || \"\").toLowerCase() === hh) return t;\n        if (raw && String(t.url || \"\").trim() === raw) return t;\n    }\n    return null;\n}\n\nfunction taskPlayUrl(t) {\n    return $().lazyRule((fid, pid, taskName, ruleTitle) => {\n        let api2 = $.require(\"115Api\");\n        let c = api2.newClient();\n        let videos = [];\n        let queue = [];\n        let seen = {};\n        let scanned = 0;\n\n        function isNoiseName(name) {\n            return /(sample|preview|trailer|teaser|promo|readme|试看|试播|预告|花絮|广告|宣传|二维码)/i.test(String(name || \"\"));\n        }\n        function addVideo(f) {\n            if (!f || f.isDirectory) return;\n            try {\n                if (api2.tool.fileKind(f.name) === \"video\") videos.push(f);\n            } catch (e) { }\n        }\n        function queueRoot(id, depth) {\n            if (id) queue.push({ id: String(id), depth: depth || 0 });\n        }\n\n        if (fid) {\n            try {\n                let root = c.getFile(String(fid));\n                if (root && root.fileId) {\n                    if (root.isDirectory) queueRoot(root.fileId, 0);\n                    else addVideo(root);\n                } else {\n                    queueRoot(fid, 0);\n                }\n            } catch (e) {\n                queueRoot(fid, 0);\n            }\n        }\n\n        if (!fid && pid) {\n            try {\n                let pr = c.getFiles(String(pid), { offset: 0, pageSize: 100, order: \"file_name\", asc: \"1\", showDir: \"1\" });\n                let pa = (pr && pr.files) || [];\n                let exact = null;\n                for (let i = 0; i < pa.length; i++) {\n                    if (String(pa[i].name || \"\") === String(taskName || \"\")) { exact = pa[i]; break; }\n                }\n                if (exact) {\n                    if (exact.isDirectory) queueRoot(exact.fileId, 0);\n                    else addVideo(exact);\n                }\n            } catch (e2) { }\n        }\n\n        while (queue.length && scanned < 500) {\n            let cur = queue.shift();\n            if (!cur || seen[cur.id]) continue;\n            seen[cur.id] = true;\n            let offset = 0, loops = 0;\n            while (loops < 10 && scanned < 500) {\n                let r;\n                try {\n                    r = c.getFiles(String(cur.id), { offset: offset, pageSize: 50, order: \"file_size\", asc: \"0\", showDir: \"1\" });\n                } catch (e3) { break; }\n                let arr = (r && r.files) || [];\n                if (!arr.length) break;\n                for (let i = 0; i < arr.length && scanned < 500; i++) {\n                    let f = arr[i];\n                    scanned++;\n                    if (f && f.isDirectory) {\n                        if (cur.depth < 3) queueRoot(f.fileId, cur.depth + 1);\n                    } else {\n                        addVideo(f);\n                    }\n                }\n                offset += 50;\n                loops++;\n                if (r.count !== undefined && offset >= r.count) break;\n            }\n        }\n\n        if (videos.length) {\n            videos.sort((a, b) => Number(b.size || 0) - Number(a.size || 0));\n            let good = videos.filter((f) => !isNoiseName(f.name) && Number(f.size || 0) >= 80 * 1024 * 1024);\n            let pool = good.length ? good : videos;\n            let first = pool[0];\n            let second = pool.length > 1 ? pool[1] : null;\n            let dominant = !second || (Number(first.size || 0) >= 500 * 1024 * 1024 && Number(first.size || 0) > Number(second.size || 0) * 1.65);\n            if (pool.length === 1 || dominant) {\n                return api2.player.resolve(JSON.stringify({ pc: first.pickCode || \"\", fid: first.fileId || \"\", name: first.name || taskName || \"\", kind: \"video\" }));\n            }\n            return \"hiker://page/115OfflineResult?rule=\" + encodeURIComponent(ruleTitle) +\n                \"&page=fypage&fid=\" + encodeURIComponent(String(fid || \"\")) +\n                \"&pid=\" + encodeURIComponent(String(pid || \"\")) +\n                \"&name=\" + encodeURIComponent(String(taskName || \"\"));\n        }\n\n        let cid = fid || pid || \"0\";\n        return \"hiker://page/115List?rule=\" + encodeURIComponent(ruleTitle) +\n            \"&page=fypage&cid=\" + encodeURIComponent(String(cid)) +\n            \"&cname=\" + encodeURIComponent(\"离线 - \" + String(taskName || \"\").slice(0, 18));\n    }, String(t.fileId || \"\"), String(t.dirId || \"\"), String(t.name || \"\"), String(MY_RULE.title || \"115.简·测试\"));\n}\n\nfunction makeTaskItem(t, current) {\n    let line = statusLine(t);\n    let title = (t.status === 2 ? \"🎬 \" : \"📄 \") + (t.name || \"离线任务\");\n    if (current) title = \"当前：\" + title;\n    let item = {\n        title: title,\n        desc: line + \" · \" + api.tool.formatSize(t.size),\n        col_type: \"text_1\",\n        url: $(\"#noLoading#\").lazyRule((hash, name) => {\n            $(\"确认删除离线任务「\" + name + \"」？\\n（不删除已下载的文件）\").confirm((h) => {\n                let api4 = $.require(\"115Api\");\n                try {\n                    api4.newClient().deleteOfflineTasks([h], false);\n                    refreshPage();\n                    return \"toast://已删除\";\n                } catch (e) { return \"toast://失败：\" + e.message; }\n            }, hash);\n            return false;\n        }, t.infoHash, t.name)\n    };\n    if (t.status === 2) {\n        item.url = taskPlayUrl(t);\n        item.desc += \" · 点击播放\";\n    }\n    return item;\n}\n\nlet currentTask = null;\nlet currentHash = \"\";\nlet autoMessage = \"\";\n\nif (focusMode) {\n    try {\n        let all = listTasks(10);\n        currentTask = findSameTask(all, autoAdd);\n        if (currentTask) {\n            currentHash = String(currentTask.infoHash || \"\");\n            autoMessage = currentTask.status === 2 ? \"已存在完成任务，直接复用\" : \"已存在相同任务，不重复添加\";\n        } else {\n            let added = client.addOfflineTaskURIs([autoAdd], \"0\");\n            currentHash = (added && added[0]) ? String(added[0]) : \"\";\n            autoMessage = \"已提交到 115 离线\";\n            if (currentHash) {\n                try {\n                    let latest = listTasks(2);\n                    for (let i = 0; i < latest.length; i++) {\n                        if (String(latest[i].infoHash || \"\").toLowerCase() === currentHash.toLowerCase()) { currentTask = latest[i]; break; }\n                    }\n                } catch (e2) { }\n            }\n        }\n    } catch (e) {\n        autoMessage = \"处理磁链失败：\" + e.message;\n    }\n}\n\nif (myPage === 1) {\n    d.push({\n        title: focusMode ? '<b>🎬 115 磁链播放</b>'.fontcolor(\"#2B6CB0\") : '<b>⚡ 离线下载</b>'.fontcolor(\"#2B6CB0\"),\n        col_type: \"rich_text\",\n        extra: { textSize: 19 }\n    });\n\n    if (focusMode) {\n        d.push({ title: autoMessage || \"正在处理磁链\", col_type: \"text_center_1\", extra: { lineVisible: false } });\n        if (currentTask) {\n            d.push(makeTaskItem(currentTask, true));\n        } else if (currentHash) {\n            d.push({ title: \"⏳ 任务已创建，等待 115 返回状态\", desc: \"info_hash: \" + currentHash, col_type: \"text_center_1\" });\n        }\n        d.push({ title: \"🔄 刷新当前状态\", col_type: \"text_2\", url: $(\"刷新\").lazyRule(() => { refreshPage(); return false; }) });\n        d.push({ title: \"📋 查看全部离线任务\", col_type: \"text_2\", url: \"hiker://page/115Offline?rule=\" + MY_RULE.title + \"&page=fypage\" });\n    } else {\n\n    d.push({\n        title: \"➕ 添加离线任务\",\n        desc: \"magnet / ed2k / HTTP，多个链接可用空格分隔\",\n        col_type: \"text_1\",\n        url: $(\"\", \"输入离线链接\").input(() => {\n            let t = String(input || \"\").trim();\n            if (!t) return \"toast://已取消\";\n            let links = t.split(/\\s+/).filter((s) => s);\n            let api2 = $.require(\"115Api\");\n            try {\n                let c = api2.newClient();\n                let old = [];\n                try {\n                    for (let p = 1; p <= 5; p++) {\n                        let r = c.listOfflineTask(p), a = (r && r.tasks) || [];\n                        if (!a.length) break;\n                        a.forEach((x) => old.push(x));\n                        if (r.pageCount && p >= r.pageCount) break;\n                    }\n                } catch (e0) { }\n                let add = [];\n                links.forEach((u) => {\n                    let same = false;\n                    let raw = String(u || \"\").trim();\n                    let m = /(?:^|[?&])xt=urn:btih:([0-9A-Fa-f]{40})/i.exec(raw);\n                    let hh = m ? String(m[1]).toLowerCase() : \"\";\n                    for (let i = 0; i < old.length; i++) {\n                        if ((hh && String(old[i].infoHash || \"\").toLowerCase() === hh) || String(old[i].url || \"\").trim() === raw) { same = true; break; }\n                    }\n                    if (!same) add.push(raw);\n                });\n                if (!add.length) return \"toast://这些任务已经存在\";\n                c.addOfflineTaskURIs(add, \"0\");\n                refreshPage();\n                return \"toast://已添加 \" + add.length + \" 个任务\";\n            } catch (e) { return \"toast://添加失败：\" + e.message; }\n        })\n    });\n\n    d.push({ title: \"🔄 刷新\", col_type: \"text_2\", url: $(\"刷新\").lazyRule(() => { refreshPage(); return false; }) });\n    d.push({\n        title: \"🗑 删除已完成任务\",\n        col_type: \"text_2\",\n        url: $(\"确认删除所有已完成的离线任务？（不删除已下载文件）\").confirm(() => {\n            let api3 = $.require(\"115Api\");\n            try {\n                let c = api3.newClient();\n                let done = [];\n                for (let p = 1; p <= 10; p++) {\n                    let r = c.listOfflineTask(p), a = (r && r.tasks) || [];\n                    if (!a.length) break;\n                    a.filter((t) => t.status === 2).forEach((t) => done.push(t));\n                    if (r.pageCount && p >= r.pageCount) break;\n                }\n                if (!done.length) return \"toast://没有已完成的任务\";\n                c.deleteOfflineTasks(done.map((t) => t.infoHash), false);\n                refreshPage();\n                return \"toast://已删除 \" + done.length + \" 个任务\";\n            } catch (e) { return \"toast://失败：\" + e.message; }\n        })\n    });\n    }\n}\n\nif (focusMode) {\n    if (myPage === 1) setResult(d);\n} else {\nlet tasks = null, errText = \"\";\ntry { tasks = client.listOfflineTask(myPage); } catch (e) { errText = e.message; }\nif (errText || !tasks) {\n    if (myPage === 1) d.push({ title: \"获取任务列表失败：\" + (errText || \"接口没有返回内容\"), col_type: \"text_center_1\" });\n    setResult(myPage === 1 ? d : []);\n} else {\n    let list = tasks.tasks || [];\n    let sig = list.length + \"|\" + (list[0] ? list[0].infoHash : \"\");\n    let sigKey = \"115OfflineSig\";\n    let lastSig = getItem(sigKey, \"\");\n    let noMore = (myPage > 1) && (list.length === 0 || sig === lastSig);\n    if (!noMore) setItem(sigKey, sig);\n    if (!list.length && myPage === 1) d.push({ title: \"（暂无离线任务）\", col_type: \"text_center_1\" });\n    list.forEach((t) => d.push(makeTaskItem(t, false)));\n    setResult(noMore ? [] : d);\n}\n}\n";
var resultRule="js:\n// 多视频离线结果：电影主片不明确时显示选集，避免“最大文件”误播剧集。\nlet d = [];\nlet api = $.require(\"115Api\");\nlet client = api.newClient();\n\nfunction q(name, dflt) {\n    let v = \"\";\n    try { v = String(getParam(name, \"\") || \"\"); } catch (e) { v = \"\"; }\n    if (v) { try { v = decodeURIComponent(v); } catch (e2) { } return v; }\n    try {\n        let mv = (typeof MY_PARAMS !== \"undefined\" && MY_PARAMS) ? MY_PARAMS[name] : undefined;\n        if (mv !== undefined && mv !== null && mv !== \"\") return String(mv);\n    } catch (e3) { }\n    return dflt || \"\";\n}\n\nlet fid = q(\"fid\", \"\");\nlet pid = q(\"pid\", \"\");\nlet taskName = q(\"name\", \"离线结果\");\nlet videos = [];\nlet queue = [];\nlet seen = {};\nlet scanned = 0;\n\nfunction addVideo(f) {\n    if (!f || f.isDirectory) return;\n    try { if (api.tool.fileKind(f.name) === \"video\") videos.push(f); } catch (e) { }\n}\nfunction queueRoot(id, depth) { if (id) queue.push({ id: String(id), depth: depth || 0 }); }\n\nif (fid) {\n    try {\n        let root = client.getFile(String(fid));\n        if (root && root.fileId) {\n            if (root.isDirectory) queueRoot(root.fileId, 0); else addVideo(root);\n        } else queueRoot(fid, 0);\n    } catch (e) { queueRoot(fid, 0); }\n}\nif (!fid && pid) {\n    try {\n        let r = client.getFiles(String(pid), { offset: 0, pageSize: 100, order: \"file_name\", asc: \"1\", showDir: \"1\" });\n        let a = (r && r.files) || [];\n        for (let i = 0; i < a.length; i++) {\n            if (String(a[i].name || \"\") === String(taskName || \"\")) {\n                if (a[i].isDirectory) queueRoot(a[i].fileId, 0); else addVideo(a[i]);\n                break;\n            }\n        }\n    } catch (e2) { }\n}\nwhile (queue.length && scanned < 500) {\n    let cur = queue.shift();\n    if (!cur || seen[cur.id]) continue;\n    seen[cur.id] = true;\n    let offset = 0, loops = 0;\n    while (loops < 10 && scanned < 500) {\n        let r;\n        try { r = client.getFiles(String(cur.id), { offset: offset, pageSize: 50, order: \"file_name\", asc: \"1\", showDir: \"1\" }); } catch (e3) { break; }\n        let a = (r && r.files) || [];\n        if (!a.length) break;\n        for (let i = 0; i < a.length && scanned < 500; i++) {\n            let f = a[i]; scanned++;\n            if (f && f.isDirectory) { if (cur.depth < 3) queueRoot(f.fileId, cur.depth + 1); }\n            else addVideo(f);\n        }\n        offset += 50; loops++;\n        if (r.count !== undefined && offset >= r.count) break;\n    }\n}\n\nvideos.sort((a, b) => String(a.name || \"\").localeCompare(String(b.name || \"\")));\nd.push({ title: '<b>🎞 ' + taskName + '</b>'.fontcolor(\"#2B6CB0\"), desc: \"识别到 \" + videos.length + \" 个视频，选择要播放的文件\", col_type: \"text_1\" });\nif (!videos.length) {\n    d.push({ title: \"没有识别到视频\", col_type: \"text_center_1\", url: \"hiker://page/115List?rule=\" + MY_RULE.title + \"&page=fypage&cid=\" + encodeURIComponent(fid || pid || \"0\") + \"&cname=\" + encodeURIComponent(taskName) });\n} else {\n    let playFn = api.player.resolve;\n    videos.forEach((f) => {\n        d.push({\n            title: \"▶ \" + f.name,\n            desc: api.tool.formatSize(f.size),\n            col_type: \"text_1\",\n            url: $().lazyRule(playFn, JSON.stringify({ pc: f.pickCode || \"\", fid: f.fileId || \"\", name: f.name || \"\", kind: \"video\" }))\n        });\n    });\n}\nsetResult(d);\n";
var foundOffline=false, foundResult=false;
for(var i=0;i<pages.length;i++){
  if(pages[i]&&pages[i].path==="115Offline"){pages[i].rule=offlineRule;foundOffline=true;}
  if(pages[i]&&pages[i].path==="115OfflineResult"){pages[i].rule=resultRule;pages[i].name="115离线结果";foundResult=true;}
}
if(!foundOffline) return "toast://原版115.简缺少115Offline页面";
if(!foundResult) pages.push({col_type:"movie_3",name:"115离线结果",path:"115OfflineResult",rule:resultRule});
rule.title="115.简·测试";
rule.author="AI&三鲜汤 · 磁链播放增强 Test3";
rule.version=2026092108;
try{
  var fr=String(rule.find_rule||"");
  fr=fr.replace("输入文件名关键词搜索；粘贴 115 分享链接可直接打开；粘贴 magnet / ed2k / HTTP 链接则离线下载","文件名 / 115分享链接 / 磁链");
  fr=fr.replace('title: "复制调用"','title: "复制磁链调用"');
  fr=fr.replace('"hiker://page/115Search?rule=" + rule + "&page=fypage&kw=" + encodeURIComponent(url);','"hiker://page/115Offline?rule=" + rule + "&page=fypage&add=" + encodeURIComponent(url);');
  rule.find_rule=fr;
}catch(e3){}
rule.pages=JSON.stringify(pages);
var out="hiker://files/cache/115_test3_rule.json";
saveFile(out,JSON.stringify(rule));
return "海阔视界首页频道规则￥home_rule_url￥"+getPath(out);
})()