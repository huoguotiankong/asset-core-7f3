(function(){
var raw=fetch("hiker://home@115.简");
if(!raw||raw==="null") return "toast://未找到原版115.简，请先保留原版";
var rule;
try{rule=JSON.parse(raw);}catch(e){return "toast://读取原版115.简失败："+e.message;}
var pages;
try{pages=JSON.parse(rule.pages||"[]");}catch(e2){return "toast://解析原版页面失败："+e2.message;}
var offlineRule="js:\n// 115Offline：离线下载（添加任务 + 任务列表）\n//\n// ⚠ 分页铁律（这张页原来两条都犯）：\n//   1. 所有固定行（标题 / 添加 / 刷新 / 删除已完成 / 自动添加结果）必须只在 MY_PAGE === 1 推送，\n//      否则最后一页之后每页仍返回这些卡片，海阔认为\"还有内容\"，下拉无限追加同一批行。\n//   2. autoAdd 也必须在第 1 页才执行。它带了副作用（真的往 115 添加任务），\n//      每下拉一次就重新添加一遍同一个磁力链 —— 比重复渲染严重得多。\nlet d = [];\nlet api = $.require(\"115Api\");\n\nfunction resolvePage() {\n    let n = 1;\n    try {\n        if (typeof MY_PAGE !== \"undefined\" && MY_PAGE !== null && MY_PAGE !== \"\") {\n            let p = parseInt(MY_PAGE, 10);\n            if (!isNaN(p) && p > 0) n = p;\n        }\n    } catch (e) { }\n    if (n === 1) {\n        try {\n            let q = String(getParam(\"page\", \"\") || \"\");\n            if (/^\\d+$/.test(q) && parseInt(q, 10) > 0) n = parseInt(q, 10);\n        } catch (e) { }\n    }\n    return n;\n}\nlet myPage = resolvePage();\n\nlet client = api.newClient();\n\n// 状态映射：-1 失败 0 等待 1 下载中 2 完成\nfunction statusLine(t) {\n    if (t.status === 2) return \"✅ 完成\";\n    if (t.status === -1) return \"❌ 失败\";\n    if (t.status === 1) {\n        let pct = t.percent > 0 ? t.percent + \"%\" : \"\";\n        let rate = t.rateDownload > 0 ? \" · \" + api.tool.formatSize(t.rateDownload) + \"/s\" : \"\";\n        let left = t.leftTime > 0 ? \" · 剩余 \" + Math.ceil(t.leftTime / 60) + \" 分钟\" : \"\";\n        return \"⬇ 下载中 \" + pct + rate + left;\n    }\n    return \"⏳ 等待中\";\n}\n\n/* ---------------- 固定行（仅第 1 页） ---------------- */\nif (myPage === 1) {\n    d.push({\n        title: '<b>⚡ 离线下载</b>'.fontcolor(\"#2B6CB0\"),\n        col_type: \"rich_text\",\n        extra: { textSize: 19 }\n    });\n\n    d.push({\n        title: \"➕ 添加离线任务\",\n        desc: \"支持 magnet / ed2k / HTTP 链接，多个用空格分隔\",\n        col_type: \"text_1\",\n        url: $(\"\", \"输入离线链接（多个用空格分隔）\").input(() => {\n            let t = String(input || \"\").trim();\n            if (!t) return \"toast://已取消\";\n            let links = t.split(/\\s+/).filter((s) => s);\n            let api2 = $.require(\"115Api\");\n            try {\n                let added = api2.newClient().addOfflineTaskURIs(links, \"0\");\n                refreshPage();\n                return \"toast://已添加 \" + added.length + \" 个任务\";\n            } catch (e) {\n                return \"toast://添加失败：\" + e.message;\n            }\n        })\n    });\n\n    // 进入页面时若带 add 参数则自动添加（来自首页搜索框；URL 查询参数用 getParam 读取）。\n    // 只在第 1 页执行：这是有副作用的操作，下拉翻页时绝不能再跑一遍。\n    let autoAdd = \"\";\n    try { autoAdd = decodeURIComponent(getParam(\"add\", \"\") || \"\").trim(); } catch (e) { autoAdd = getParam(\"add\", \"\") || \"\"; }\n    if (autoAdd) {\n        try {\n            client.addOfflineTaskURIs([autoAdd], \"0\");\n            d.push({\n                title: \"✅ 已自动添加任务：\" + autoAdd.slice(0, 60),\n                col_type: \"text_center_1\",\n                extra: { lineVisible: false }\n            });\n        } catch (e) {\n            d.push({\n                title: \"⚠ 自动添加失败：\" + e.message,\n                col_type: \"text_center_1\",\n                extra: { lineVisible: false }\n            });\n        }\n    }\n\n    d.push({\n        title: \"🔄 刷新\",\n        col_type: \"text_2\",\n        url: $(\"刷新\").lazyRule(() => { refreshPage(); return false; })\n    });\n    d.push({\n        title: \"🗑 删除已完成任务\",\n        col_type: \"text_2\",\n        url: $(\"确认删除所有已完成的离线任务？（不删除已下载文件）\").confirm(() => {\n            let api3 = $.require(\"115Api\");\n            try {\n                let c = api3.newClient();\n                let done = (c.listOfflineTask(1).tasks || []).filter((t) => t.status === 2);\n                let page = 2;\n                while (done.length && page <= 10) {\n                    let more = c.listOfflineTask(page).tasks || [];\n                    if (!more.length) break;\n                    more.filter((t) => t.status === 2).forEach((t) => done.push(t));\n                    page++;\n                }\n                if (!done.length) return \"toast://没有已完成的任务\";\n                c.deleteOfflineTasks(done.map((t) => t.infoHash), false);\n                refreshPage();\n                return \"toast://已删除 \" + done.length + \" 个任务\";\n            } catch (e) { return \"toast://失败：\" + e.message; }\n        })\n    });\n}\n\n/* ---------------- 任务列表 ---------------- */\nlet tasks = null, errText = \"\";\ntry {\n    tasks = client.listOfflineTask(myPage);\n} catch (e) {\n    errText = e.message;\n}\n\nif (errText || !tasks) {\n    // 出错提示也是卡片，同样只能在第 1 页推，否则又是一个\"永远有内容\"的源头\n    if (myPage === 1) {\n        d.push({ title: \"获取任务列表失败：\" + (errText || \"接口没有返回内容\"), col_type: \"text_center_1\" });\n    }\n    setResult(myPage === 1 ? d : []);\n} else {\n    let list = tasks.tasks || [];\n\n    // 翻页终止判定：无数据，或与上次请求完全相同（接口忽略页码时）都要截断\n    let sig = list.length + \"|\" + (list[0] ? list[0].infoHash : \"\");\n    let sigKey = \"115OfflineSig\";\n    let lastSig = getItem(sigKey, \"\");\n    let noMore = (myPage > 1) && (list.length === 0 || sig === lastSig);\n    if (!noMore) setItem(sigKey, sig);\n\n    if (!list.length && myPage === 1) {\n        d.push({ title: \"（暂无离线任务）\", col_type: \"text_center_1\" });\n    }\n\n    list.forEach((t) => {\n        let line = statusLine(t);\n        let item = {\n            title: (t.status === 2 ? \"🎬 \" : \"📄 \") + t.name,\n            desc: line + \" · \" + api.tool.formatSize(t.size),\n            col_type: \"text_1\",\n            url: $(\"#noLoading#\").lazyRule((hash, name) => {\n                $(\"确认删除离线任务「\" + name + \"」？\\n（不删除已下载的文件）\").confirm((h) => {\n                    let api4 = $.require(\"115Api\");\n                    try {\n                        api4.newClient().deleteOfflineTasks([h], false);\n                        refreshPage();\n                        return \"toast://已删除\";\n                    } catch (e) { return \"toast://失败：\" + e.message; }\n                }, hash);\n                return false;\n            }, t.infoHash, t.name)\n        };\n        // 已完成任务：优先按 file_id 自动定位下载结果并直接播放主视频。\n        // 115 的 wp_path_id(dirId) 是“父目录”，旧逻辑跳它会先回到网盘根/父层，\n        // 用户还要再点任务目录、再点视频；真正的任务结果应优先从 fileId 开始。\n        if (t.status === 2) {\n            item.url = $().lazyRule((fid, pid, taskName, ruleTitle) => {\n                let api2 = $.require(\"115Api\");\n                let c = api2.newClient();\n                let videos = [];\n                let queue = [];\n                let seen = {};\n                let scanned = 0;\n\n                function addFile(f) {\n                    if (!f || f.isDirectory) return;\n                    try {\n                        if (api2.tool.fileKind(f.name) === \"video\") videos.push(f);\n                    } catch (e) { }\n                }\n\n                // fileId 可能直接就是视频，也可能是本次离线生成的任务目录。\n                if (fid) {\n                    try {\n                        let root = c.getFile(String(fid));\n                        if (root && root.fileId) {\n                            if (root.isDirectory) queue.push({ id: String(root.fileId), depth: 0 });\n                            else addFile(root);\n                        } else {\n                            queue.push({ id: String(fid), depth: 0 });\n                        }\n                    } catch (e) {\n                        queue.push({ id: String(fid), depth: 0 });\n                    }\n                }\n\n                // 最多递归 4 层 / 扫 500 项，避免超大任务把页面拖死。\n                while (queue.length && scanned < 500) {\n                    let cur = queue.shift();\n                    if (!cur || seen[cur.id]) continue;\n                    seen[cur.id] = true;\n                    let offset = 0;\n                    let loops = 0;\n                    while (loops < 10 && scanned < 500) {\n                        let r;\n                        try {\n                            r = c.getFiles(String(cur.id), {\n                                offset: offset, pageSize: 50, order: \"file_size\", asc: \"0\", showDir: \"1\"\n                            });\n                        } catch (e) { break; }\n                        let arr = (r && r.files) || [];\n                        if (!arr.length) break;\n                        for (let i = 0; i < arr.length && scanned < 500; i++) {\n                            let f = arr[i];\n                            scanned++;\n                            if (f && f.isDirectory) {\n                                if (cur.depth < 3) queue.push({ id: String(f.fileId), depth: cur.depth + 1 });\n                            } else {\n                                addFile(f);\n                            }\n                        }\n                        offset += 50;\n                        loops++;\n                        if (r.count !== undefined && offset >= r.count) break;\n                    }\n                }\n\n                if (videos.length) {\n                    // 默认播放体积最大的主视频：可自动避开常见 sample/预告小文件。\n                    videos.sort((a, b) => Number(b.size || 0) - Number(a.size || 0));\n                    let f = videos[0];\n                    return api2.player.resolve(JSON.stringify({\n                        pc: f.pickCode || \"\",\n                        fid: f.fileId || \"\",\n                        name: f.name || taskName || \"\",\n                        kind: \"video\"\n                    }));\n                }\n\n                // 没识别到视频时才退回目录浏览。fileId 优先，dirId 只作最后兜底。\n                let cid = fid || pid || \"0\";\n                return \"hiker://page/115List?rule=\" + encodeURIComponent(ruleTitle) +\n                    \"&page=fypage&cid=\" + encodeURIComponent(String(cid)) +\n                    \"&cname=\" + encodeURIComponent(\"离线 - \" + String(taskName || \"\").slice(0, 18));\n            }, String(t.fileId || \"\"), String(t.dirId || \"\"), String(t.name || \"\"), String(MY_RULE.title || \"115.简·测试\"));\n            item.desc = line + \" · \" + api.tool.formatSize(t.size) + \" · 点击直接播放主视频\";\n        }\n        d.push(item);\n    });\n\n    // 返回空列表 = 通知海阔\"没有更多了\"，停止下拉加载\n    setResult(noMore ? [] : d);\n}\n";
var found=false;
for(var i=0;i<pages.length;i++){
  if(pages[i]&&pages[i].path==="115Offline"){pages[i].rule=offlineRule;found=true;break;}
}
if(!found) return "toast://原版115.简缺少115Offline页面";
rule.title="115.简·测试";
rule.author="AI&三鲜汤 · 磁链直播放增强 Test2";
rule.version=2026092107;
// 修正原版复制调用：磁链调用应进入离线页，而不是普通搜索页。
try{
  rule.find_rule=String(rule.find_rule||"").replace(
    '"hiker://page/115Search?rule=" + rule + "&page=fypage&kw=" + encodeURIComponent(url);',
    '"hiker://page/115Offline?rule=" + rule + "&page=fypage&add=" + encodeURIComponent(url);'
  );
}catch(e3){}
rule.pages=JSON.stringify(pages);
return "海阔视界，首页频道￥home_rule￥"+JSON.stringify(rule);
})()