# 115.简 / Pan115 开发记录

状态：**Stable 1.2.0 / Build 2026092220 / 已实机验证；Test 1.2.1-test.19 / Build 2026092239 / 紧急修复待验证**  
首次纳入：2026-09-21  
最近更新：2026-09-22

## 1.2.1-test.19 / Build 2026092239 — 文件管理 JS 编译错误紧急修复

用户实机覆盖 Test18 后，打开“文件管理”立即报错：

```text
解析失败！JS编译出错：在属性列表的后面缺少 “}”（JSEngine#68）
```

复核 Test18 安装器确认，问题来自“最近播放 / 视频收藏”阶段对 `115FileManage` 进行**运行时源码字符串注入**。其中视频收藏 `longClick` 代码的对象/属性闭合不平衡，导致整个文件管理页面在 JS 编译阶段失败，属于发布前静态语法校验缺失造成的回归。

Test19 不继续在故障代码上打补丁，而是先恢复稳定边界：

- `115FileManage` **完整恢复到用户已确认正常的 Test17 `file_manage_v9.js`**；
- 保留独立 `115PlayHistory` 页面模块，避免已生成的本地历史数据丢失；
- 暂时撤销“文件管理播放时自动记录历史”和“文件长按收藏视频”的注入钩子；
- 搜索、离线中心、账号中心、回收站、登录、磁链 focusMode、文件操作协议全部不动；
- Test19 可从已损坏的 Test18 直接同名覆盖，也可从 Test17 直接覆盖。

长期新增禁用项：**稳定页面不得再通过多段字符串 `replace/slice` 拼接复杂 JS 属性/长按对象后直接发布。复杂功能应生成完整模块文件，并在发布前至少做一次 JS 语法解析校验。**

状态：`pending-device-validation-hotfix`。

## 1.2.1-test.18 / Build 2026092238 — 最近播放 / 视频收藏 Phase 1（实机失败）

目标是增加最近播放、视频收藏与播放中心。本地数据方案本身不需要额外 115 请求，但实现采用安装器运行时修改文件管理源码。

实机结果：**失败**。文件管理在页面加载前即发生 JS 编译错误，无法使用。根因是视频收藏 `longClick` 注入表达式缺少正确的对象闭合。该版由 Test19 紧急修复替代，不再作为可测试基线。

## 1.2.1-test.17 / Build 2026092237 — 文件管理 UX Phase 3（已实机验证）

用户明确反馈 Test17“正常”。因此文件管理 V9 作为当前恢复基线。

新增能力：

- 文件管理顶部增加紧凑 `⭐ 常用` 入口；
- 新增独立 `115FileShortcuts` 常用目录页；
- 进入文件夹时本地记录最近访问，最多 12 条，不增加任何 115 网络请求；
- 文件夹长按可 `收藏目录 / 取消收藏目录`，最多保留 20 个；
- 常用目录页分为 `⭐ 收藏目录` 与 `🕘 最近访问`；
- 收藏/最近记录保存完整 breadcrumb trail；
- 视频 / 图片 / 音频 / 文档 / 压缩包 / 其它使用轻量图标；
- 继续沿用现有名称/时间/大小排序与类型筛选记忆；
- 新建、批量、重命名、复制、移动、删除到回收站底层合同不变。

模块：

- `apps/cloud/pan115/modules/file_manage_v9.js`
- `apps/cloud/pan115/modules/file_shortcuts_v1.js`

状态：`device-validated-file-management-phase3`。

## 1.2.1-test.16 / Build 2026092236 — 账号 / 容量 / 设置中心 Phase 1（已实机验证）

用户实机反馈：**正常**。

新增：

```text
115AccountCenter  账号与设置
115AccountDiag    诊断与高级设置
```

能力：登录状态、用户名/头像/VIP、容量总量/已用/剩余/使用率（当前 Client 支持时）、文件/离线/回收快捷入口、离线保存目录摘要、原登录/扫码/切换账号入口、安全诊断页。

安全边界：原 `115Account` 登录链不替换；诊断页不显示 Cookie / SEID / KID；无后台轮询；搜索、文件管理、回收站和外部磁链协议不改。

状态：`device-validated-account-center-phase1`。

## 1.2.1-test.15 / Build 2026092235 — 离线中心 Phase 2

继续 Test14 的低噪音 UI：短 `粘贴链接` 输入、隐藏 CID/mode/hash；增加下载进度、速度、剩余时间、任务时间；完成任务显式点击时尝试保守单视频一步播放，多视频/嵌套目录回落既有 `115OfflineResult`。

安全边界：页面加载一次 `listOfflineTask(page)`；禁止后台/并发轮询；单视频快路径只在用户点击时执行；外部 `115Offline?add=` focusMode 不动。

## 1.2.1-test.14 / Build 2026092234 — 离线中心 UI 精简

- 输入框只显示 `粘贴链接`；
- CID / hiker-cache / raw info_hash 从普通页面移除；
- 技术信息下沉长按/诊断；
- 页面/入口标题缩短为 `离线中心`。

全项目 UI 规则：**搜索框/输入框只承担输入，默认短提示；协议说明、调试参数、CID/Hash 等不得常驻正常页面。**

## 1.2.1-test.13 / Build 2026092233 — 离线中心 Phase 1（部分实机验证）

实机截图确认：离线中心可打开，任务列表可显示，状态/大小有数据，保存目录识别为 `海阔视界`，筛选/刷新/文件/回收入口正常。

新增：全部/进行中/已完成/失败筛选、手动刷新、新建 magnet/ed2k/HTTP(S) 离线、完成结果入口、移除任务记录（保留文件）、失败任务重试。

永久安全要求：无后台高频轮询；每页一次任务列表请求；目录扫描只在显式提交且无缓存时执行。

## 1.2.1-test.12 / Build 2026092232 — 搜索目录跳转闭环（已实机验证）

最终确认当前 115Api 规范化目录使用：

```text
isDirectory=true + fileId → fileId 即目录 ID
```

原始 WebAPI 对象仍可能是 `!fid + cid → cid 为目录 ID`。

实机通过：`ipx-641` 保持 6 条、视频可播放、关键词摘要纯文本、目录可进入。

**Test9 恢复的旧搜索请求链是不可破坏基线。** 后续搜索只能改结果层/字段解释，不重新合成请求。

## 1.2.1-test.5 ～ Test11 — 搜索失败与收敛记录

- Test5～8：自行重构/合成 `/files/search` 后已知关键词从约 6 条退化为 0；
- Test9：从本地旧规则恢复原始搜索链，6 条结果和视频播放恢复；
- Test10：只修结果层，关键词 HTML 裸显修复，但目录 ID 仍失败；
- Test11：捕获同一次旧搜索返回值，但错误要求规范化目录额外存在 `cid`；
- Test12：改为 `isDirectory + fileId` 后目录跳转闭环。

长期结论：**不能根据 endpoint / aid / 认证模式自行拼一个“看起来等价”的 115 搜索请求。**

## 1.2.1-test.4 / Build 2026092224 — 文件管理 UX 基线

文件管理 V8：根目录/上一级/面包屑、名称/时间/大小排序、类型筛选、文件信息页、目标目录导航和最近目标。

## 1.2.1-test.3 / Build 2026092223 — 新建 / 重命名 / 单项复制移动

增加新建文件夹；长按重命名、复制、移动、删除到回收站。新建/重命名隔离在 `file_ops_v1.js`，复制/移动复用批量核心。

## 1.2.1-test.2 / Build 2026092222 — 批量管理（已实机验证）

多选/取消/全选、批量删除到回收站、批量复制、批量移动、目标目录选择已通过实机。顺序分批执行，不并发，一次最多 1000 项。

## 1.2.1-test.1 / Build 2026092221 — 清空回收站（已实机验证）

115 安全密码 → 二次确认 → 快照回收站 rid → `cleanRecycleBin` 顺序分批；失败立即停止并报告进度。

## Stable 1.2.0 / Build 2026092220 — 已实机验证

稳定基线：普通删除到回收站、回收站列表/还原/永久删除、磁链低频安全链、独立文件管理、登录/Cookie/m115、分享与个人文件直链播放。

普通删除最终采用：捕获已验证 `revertRecycleBin()` 的真实认证 request，只将 `/rb/revert` 改 `/rb/delete`、`rid` 改 `fid[n]`。

## 长期协议与禁用项

外部磁链固定：

```js
function playBy115(url) {
    if (!url) return "toast://未获取到磁力链接";
    return "hiker://page/115Offline?rule=115.简&page=fypage&add=" + encodeURIComponent(url);
}
```

永久禁止/避免：

- magnet → `115Search?kw=`；
- 自行重写完整 115 magnet 协议层；
- 后台高频/并发轮询；
- 普通删除猜 `client.request()` 参数顺序；
- 重写 Test9 已恢复并实机证明工作的搜索请求链；
- 点击目录后另发猜测搜索恢复 ID；
- 把 `fileId` 固定解释为普通文件 ID；
- 普通页面常驻 Cookie/CID/hash/endpoint 等技术字段；
- 通过字符串拼接/替换复杂页面 JS 后未经语法校验直接发布；
- 未经实机验证就标记完整 `deviceValidated`。

## 当前开发边界

Stable 1.2.0 不动。Test19 先恢复 Test17 已验证的文件管理 V9，最近播放/视频收藏暂停集成。**必须先确认文件管理恢复正常，再重新以完整模块方式实现播放历史功能，不再运行时拼接复杂 JS。**
